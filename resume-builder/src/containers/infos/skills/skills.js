import React from 'react';
import Input from '../../../UI/input/input';
const skills = (props) => {
    let skills = [];
    console.log(props.skills);
    // {
    //     // skills.push({
    //     //     id: props.skills[key].id,
    //     //     value: props.skills[key].value,
    //     //     config: props.skills[key]
    //     // });
    //     console.log(key);
    // }
    // console.log(skills);

    // let input = skills.map((s)=>{
    //     return <Input
    //     key={s.id}
    //     elementType={s.config.elementType}
    //     elementConfig={s.config.elementConfig}
    //     value={s.config.value}
    //     invalid={!s.config.valid}
    //     touched={s.config.touched}
    //     shouldValidate={s.config.validation}
    //     changed={(event)=>this.onChangeHandler(event, s.id)} />
    // })
    return (
        <>
        <div>

        </div>
        </>
    );
}

export default skills;